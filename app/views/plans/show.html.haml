.row.justify-content-center.align-items-center
  .col-md-8.col-11
    %h1.fs-2.text-center.my-4 #{@plan.name}プラン
    %p.mb-3= @plan.description
    %legend.fs-4= Plan.human_attribute_name(:ingredient_sets)
    .row.row-cols-1.g-4
      - @plan.ingredient_sets.default_order.each do |ingredient_set|
        .col= render 'ingredient_set', ingredient_set:
