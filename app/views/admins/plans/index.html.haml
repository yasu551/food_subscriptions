.row.justify-content-center.align-items-center
  .col-md-8.col-11
    %h1.fs-2.text-center.my-4 プラン一覧
    .card-group.justify-content-around
      - @plans.each do |plan|
        = link_to edit_admins_plan_path(plan), class: 'text-decoration-none' do
          = render 'plan', plan:
