.row.justify-content-center.align-items-center
  .col-md-8.col-11
    %h1.fs-2.text-center.my-4 食材セット一覧
    .mb-3= link_to '新規作成', new_admins_ingredient_set_path, class: 'btn btn-primary'
    %ul.list-group
      - @ingredient_sets.each do |ingredient_set|
        %li.list-group-item
          = link_to edit_admins_ingredient_set_path(ingredient_set) do
            %p.mb-0= ingredient_set.name
